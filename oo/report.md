# 双拼输入统计结果

本报告由 `analyze_double_pinyin.py` 根据当前目录的清洗数据、小说文本和键盘坐标生成。

## 统计口径

- 全拼按清洗后的无声调拼音字符串长度计数；双拼按一个声母键加一个韵母键计数。
- 多音字以数据中的基准读音计算，同时保留全拼最短/最长读音供敏感性分析。双拼变体均按各方案重新编码。
- 选词情景暂按每个汉字额外增加 1 次击键，不模拟具体输入法词库、候选排序或自动上屏。
- 电脑距离按标准指法和每根手指自己的 Home Row 初始位置计算；手机距离只累加相邻按键之间的折线距离。
- 小说移动距离按去掉“来源：...”元数据后的正文顺序计算；缺少拼音的汉字会跳过并列出。
- 按用户确认，手机微软双拼的分号触点位于大写/Shift 键位置；本次手机距离按物理触点路径计算，不额外计入切换大写层的动作。
- 当前清洗数据的粒度是单字，不是分词后的词；因此本文结果按汉字频率加权，不能直接称为词频结果。

## 方案核对

| 全拼 | 微软双拼 | 小鹤双拼 |
|---|---|---|
| `tong` | `ts` | `ts` |
| `chou` | `ib` | `iz` |
| `shuang` | `ud` | `ul` |
| `shi` | `ui` | `ui` |
| `ni` | `ni` | `ni` |
| `you` | `yb` | `yz` |
| `yuan` | `yr` | `yr` |
| `yue` | `yt` | `yt` |

因此 `统筹 = tong chou = ts ib = tsib`。映射依据：
微软：https://raw.githubusercontent.com/rime/rime-double-pinyin/master/double_pinyin_mspy.schema.yaml
小鹤：https://raw.githubusercontent.com/rime/rime-double-pinyin/master/double_pinyin_flypy.schema.yaml

## 加权击键数

| 对象 | 总权重 | 全拼 | 微软双拼 | 小鹤双拼 | 微软节省 | 小鹤节省 |
|---|---:|---:|---:|---:|---:|---:|
| junda | 193,504,018 | 2.9797 | 2.0000 | 2.0000 | 0.9797 | 0.9797 |
| 三体 | 768,132 | 2.9993 | 2.0000 | 2.0000 | 0.9993 | 0.9993 |
| 最后一个小说 | 1,613,101 | 2.8879 | 2.0000 | 2.0000 | 0.8879 | 0.8879 |
| 三体 I：地球往事 | 163,772 | 2.9936 | 2.0000 | 2.0000 | 0.9936 | 0.9936 |
| 三体 II：黑暗森林 | 278,220 | 2.9902 | 2.0000 | 2.0000 | 0.9902 | 0.9902 |
| 三体 III：死神永生 | 326,140 | 3.0100 | 2.0000 | 2.0000 | 1.0100 | 1.0100 |
| 福尔摩斯探案全集图注本套装共9册 | 1,613,101 | 2.8879 | 2.0000 | 2.0000 | 0.8879 | 0.8879 |

## 结果自检与解释

平均击键数的差距并不小：Junda、三体、福尔摩斯分别节省约 0.98、1.00、0.89 键/字，约占全拼的 30.7%—33.3%。
全拼平均约为 2.9—3.0 键/字，是因为这里统计的是单字拼音长度；普通汉字的拼音多为 2—3 个字母，`统筹` 的 `tonchou`（4 键/字）属于较长样例，不能代表平均值。
双拼在本口径下每个有拼音汉字固定为 2 键，因此不同数据集之间的双拼平均值都会是 2.0000；差异主要来自各数据集的全拼音节长度分布。
全拼和双拼的平均值使用同一批有拼音汉字作分母；三组有效字频覆盖率均超过 99.98%，缺失拼音不会被擅自当作 0 键。

## 选词情景

在当前简化模型里，每个汉字额外选择一次候选字，因此双拼从 2 次变为 3 次；完整结果见 `summary.csv`。

## 小说移动距离

完整数值见 `movement.csv`。电脑单位同时给出原始截图像素和按 36.5 px 键距归一化的结果；手机按 142 px 左右的字母键距归一化。不同设备不直接比较原始像素。

正文与 CSV 频数的逐书核对见 `frequency_reconciliation.csv`；清洗时排除的来源标题行不会进入正文统计。

## 输入句测试

测试句：`你这个 ui 有问题啊，你在看看，记得调用子代理用 luna`。完整输入码见 `phrase_test.csv`。

## 数据异常

逐字表共 23,024 行；方案编码异常见 `anomalies.csv`。原始 CSV 中的缺失拼音与参考表未收录字符保持原状，不在本脚本中擅自补读音。
